{
  "name": "@genoffice/renderer-bridge",
  "version": "0.1.0",
  "private": true,
  "description": "GenOffice renderer compatibility bridge — explicit typed object factories that return the existing window.* API shapes, delegating to domain services and platform capabilities. Pure factories; no window mutation inside this package.",
  "license": "Apache-2.0",
  "type": "module",
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "scripts": {
    "test": "vitest run",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "@genoffice/runtime-contracts": "*",
    "@genoffice/platform": "*"
  },
  "devDependencies": {
    "typescript": "^5.9.0",
    "vitest": "^3.0.0"
  }
}
